# eclipse
bin
*.launch
.settings
.metadata
.classpath
.project
.eclipse

# vscode / cursor
.vscode

# idea
out
*.ipr
*.iws
*.iml
.idea/*
!.idea/scopes

# gradle
build
.gradle

# other
eclipse
run
# training runs at the root, and the loaders' own run folders
runs
# the Java and Python setup.ps1 downloads when a machine has none of its own
.tools
# worktrees Claude Code makes for work in parallel
.claude/worktrees

# python
__pycache__
*.pyc
.venv
venv
checkpoints
logs

# the mind subproject (mind/), a pure-Python testbed with no Java in it
mind/runs/
mind/text/data/raw/
mind/text/data/dialogue_partial.jsonl
mind/text/data/generated_raw/
# trained checkpoints are regenerable; the exported weights.npz + model.json are what ships
mind/text/models/**/model.pt
mind/text/models/**/*.log
__pycache__/
.pytest_cache/
