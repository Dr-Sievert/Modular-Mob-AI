# eclipse
bin
*.launch
.settings
.metadata
.classpath
.project
.eclipse

# vscode / cursor
.vscode

# idea
out
*.ipr
*.iws
*.iml
.idea/*
!.idea/scopes

# gradle
build
.gradle

# other
eclipse
run
# training runs at the root, and the loaders' own run folders
runs
# the Java and Python setup.ps1 downloads when a machine has none of its own
.tools
# worktrees Claude Code makes for work in parallel
.claude/worktrees

# python
__pycache__
*.pyc
.venv
venv
checkpoints
logs
