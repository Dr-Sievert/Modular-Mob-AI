# Port

The brief for bringing the mind's two trained models into the combat mod lives with the half that wrote them:

**[`mind/docs/port.md`](../mind/docs/port.md)** — which `Brain` each model becomes, the `.mbw` segment order proposed for
each, the one seam in `BrainState` that has to open, the chat hook, and the parity procedure.

The frozen artefacts it describes are in [`shared/models/`](../shared/README.md), and the format and schema-id rules both
sides must agree on are in [`shared/README.md`](../shared/README.md).

This file is a pointer, so that somebody landing in the repository root finds the brief without knowing which half owns
it. It is not a second copy: nothing about the port is written down here.
