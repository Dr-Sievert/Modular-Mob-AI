numpy>=1.26

# The network. This line gets the CPU build, which is enough to run everything. For the GPU, install torch from the
# matching CUDA index first and this line will leave it alone:
#
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
#
torch>=2.2
